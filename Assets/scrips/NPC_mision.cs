using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class NPC_mision : MonoBehaviour
{
    private void Update()
    {
        if (Input.GetKeyDown(KeyCode.F))
        {
            Debug.Log("se presiono f");
        
        }
    }
}
